server:
  port: 18084

spring:
  cloud:
    nacos:
      discovery:
        ip: 127.0.0.1
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: idevelop-auth
          uri: lb://idevelop-auth
          predicates:
            - Path=/idevelop-auth/**
    loadbalancer:
      retry:
        enabled: true


