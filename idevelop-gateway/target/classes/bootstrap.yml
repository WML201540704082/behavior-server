server:
  port: 18084

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: idevelop-auth
          uri: lb://idevelop-auth
          predicates:
            - Path=/idevelop-auth/**
    loadbalancer:
      retry:
        enabled: true


